
cond with =>

scan out defines in binding constructs (internal definitions)

delay & force. Needed by some macro systems.

call-with-*-file, with-*-file

revisit quasiquote

lowlevel unhygienic lisp-like macros that i actually understand

call it quits and move on to the compiler
